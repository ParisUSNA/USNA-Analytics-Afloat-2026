let
    Source = SharePoint.Files("https://flankspeed.sharepoint-mil.us/sites/httpsflankspeed.sharepoint-mil.ussitesUSFF-INSURV-HQ-CUI", [ApiVersion = 15]),
    #"Filtered Rows" = Table.SelectRows(Source, each ([Extension] = ".xls" or [Extension] = ".xlsm" or [Extension] = ".xlsx")),
    #"Filtered Rows1" = Table.SelectRows(#"Filtered Rows", each Text.Contains([Name], "INSURV INSPECTOR LIST (IIL) MASTER")),
    #"Inserted Text Between Delimiters" = Table.AddColumn(#"Filtered Rows1", "Text Between Delimiters", each Text.BetweenDelimiters([Folder Path], "/", "/", {2, RelativePosition.FromEnd}, 0), type text),
    #"Removed Other Columns" = Table.SelectColumns(#"Inserted Text Between Delimiters",{"Text Between Delimiters", "Name", "Content"}),
    #"Filtered Hidden Files1" = Table.SelectRows(#"Removed Other Columns", each [Attributes]?[Hidden]? <> true),
    #"Invoke Custom Function1" = Table.AddColumn(#"Filtered Hidden Files1", "Transform File", each #"Transform File"([Content])),
    #"Removed Other Columns1" = Table.SelectColumns(#"Invoke Custom Function1", {"Transform File"}),
    #"Expanded Table Column1" = Table.ExpandTableColumn(#"Removed Other Columns1", "Transform File", Table.ColumnNames(#"Transform File"(#"Sample File")))
in
    #"Expanded Table Column1"